// Main exports
export { Logger } from './core/logger'

// Type exports
export type { LogLevel, LogEntry, LoggerConfig, UploadStrategy, StorageInterface, UploaderInterface } from './types'

// Default export
export { Logger as default } from './core/logger'
